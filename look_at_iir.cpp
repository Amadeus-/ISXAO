#include "isxao_main.h"
#include "look_at_iir.h"

namespace ao
{
#if true
	FUNCTION_AT_ADDRESS(look_at_iir::look_at_iir(const identity_t*, const identity_t*, int), look_at_iir_t__look_at_iir_t)
#else
	static_assert(false, "look_at_iir::look_at_iir(const identity_t*, const identity_t*, int) does not have a native function.");
#endif

}