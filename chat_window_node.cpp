#include "isxao_main.h"
#include "chat_window_node.h"

namespace ao
{
	
#if true
	// ReSharper disable once CppMemberFunctionMayBeStatic
	// ReSharper disable once CppMemberFunctionMayBeConst
	FUNCTION_AT_ADDRESS(bool chat_window_node::parse_text_command(string&), chat_window_node_c__parse_text_command)
#else
	static_assert(false, "chat_window_node::parse_text_command(string&) requires a native function.");
#endif

}